[user]
  name = Harry Robertson
  email = harry.robertson@gmail.com
[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = auto
[branch]
  autosetuprebase = always
[push]
  default = tracking
[pull]
  rebase = true
[gc]
  # Don't run gc --auto for me after some commands.
  # I will run git-gc from time to time by hand.
  auto = 0

[alias]
  co = checkout
  cp = cherry-pick
  c = commit
  s = status
  b = branch
  a = add
  d = diff
  ss = stash
  sp = stash pop

  l = log --pretty=format:\"%h %ad | [%an] %s%d\" --graph --date=short
  ls = log -n 5 --graph --decorate --oneline --abbrev-commit --date=relative --pretty=format:'%C(auto)%h %C(bold blue)%an%Creset %Cgreen(%cr)%Creset %s %C(yellow)%d%Creset'
  last = "!git log --oneline -n 1 --format='%H'"

[core]
  excludesfile = /Users/harry/.gitignore
  editor = /usr/bin/vim
  # Install delta with `brew install git-delta`
  pager = delta

[interactive]
  diffFilter = delta --color-only
[delta]
  navigate = true  # use n and N to move between diff sections
  dark = true      # or light = true, or omit for auto-detection
  line-numbers = true
  #side-by-side = true
[merge]
  conflictstyle = zdiff3
[init]
	defaultBranch = main
